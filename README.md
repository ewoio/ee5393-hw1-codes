The main function is the same as original one.

The files `aleae_main.cc` and `aleae_stoch.cc` were modified for
Question 1 – Part (b) to implement the required functionality.
However, these modifications have been commented out so that the
original behavior of the simulator is preserved for the remaining
questions.

The repository also contains all reaction files (`*.r`) and
input files (`*.in`) used in the simulations reported in this
assignment.# ee5393-hw1-codes
